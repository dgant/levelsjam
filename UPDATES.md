# Updates

- Do not add speculative HUD text, branding captions, or click-to-enter gates unless the user explicitly asks for them. The default interaction model for this game should start immediately on page load.
- When the scene renders black, anchor atmosphere changes to a documented known-good `@takram/three-atmosphere` lighting example instead of inventing new exposure or lighting settings from scratch.
- When a source page like ShareTextures is provided for a material, use the linked downloadable asset pack rather than the page's preview image unless the user explicitly asks for the preview itself.
- When the user asks for the official three.js `Water`, document that requirement explicitly rather than approximating it with a generic translucent plane.
- When tuning movement, keep the documented speed, acceleration, deceleration, gravity, and jetpack targets in the spec so the controller and tests stay aligned.
- When a key is assigned to a requested tooling or debug control, reserve that key from generic gameplay relock handling instead of letting it fall through to pointer-lock logic.
- When a sun-linked control is requested, keep the atmosphere sun and sky light defaults coupled unless the user explicitly asks for them to diverge.
- When benchmarking startup and test duration, write the target in the docs next to the measurement so regressions can be judged quickly.
- When the repository root is both a local Vite entrypoint and a committed Pages bundle, keep the root `index.html` dual-mode so the approved local dev ports load `/src/main.tsx` while static preview and Pages keep loading the committed bundle.
- When this repository must keep GitHub Pages output committed at the root, treat `public/textures` as the only authoritative texture source and sync root `textures/` from there instead of copying them back out of `dist`.
- When a startup regression appears, inspect the resource waterfall before changing rendering code. In this project the dominant late-startup requests are remote `@takram/three-atmosphere` EXR assets rather than local Vite startup.
- When benchmarking browser tests for the one-minute target, prepare the published build once and time the Playwright runner separately from that build step.
- When a third-party runtime asset shows up as a startup bottleneck, vendor it into same-origin project assets before spending time on more exotic rendering changes.
- When using Playwright or benchmark scripts, reliably shut down any headless Chrome processes you started once the task is complete.
- When a crash or forced restart can interrupt local tooling, make the tool recover from corrupted state files instead of assuming JSON state is always intact.
- When using `@takram/three-atmosphere` light-source lighting, treat the package as the authority for sun color. Reuse the atmosphere-derived `SunLight` color for water and related sun effects instead of feeding those systems a manual white sun.
- When a UI light-strength control defaults to a non-unit value for usability, normalize it back around the rendering library's default baseline before applying it to actual light intensities.
- When the user asks for EV calibration in this project, implement it as an explicit scene-relative EV control with `EV 0` at the calibrated default look and one-stop doubling/halving of exposure per EV step.
- When smoke-test time regresses, trim browser harness overhead before touching app code. In this repository, dropping expensive screenshot comparisons and reducing the Playwright viewport produced a large win.
- When the user asks for physically anchored lighting controls in this project, expose direct sun illuminance in lux and exposure in EV100, then convert those UI values through a documented calibration layer instead of pretending the renderer's internal units are already photometric.
- When using `three-atmosphere` for this project, treat the package as the atmosphere color authority and scale its solar-irradiance uniforms, direct light, sky fill, and PMREM environment together from the same lux input so the sky, water, and surfaces stay matched.
- When the user replaces the lighting stack, remove the old controls and spec language instead of leaving dead atmosphere or sun settings behind.
- When a local loading overlay is required, keep it outside the canvas and let Suspense-controlled asset loading decide when the fade-out starts.
- When external material or HDRI sources are requested for this project, download and commit the actual assets under `public/textures` so smoke tests can verify same-origin loads.
- When the user links a specific flipbook or sprite atlas for this project, treat that linked asset as required source content rather than a style reference or invitation to generate a placeholder.
- When optimizing performance, measure the delta and keep only obvious free wins or changes that demonstrably improve the measured result; do not optimize blindly.
- When calibrating HDRI-based lighting in this project, keep one fixed HDRI calibration constant and one shared internal light-unit scale. Do not tie exposure semantics to the current default slider value.
- When running Playwright smoke tests repeatedly in this repository, wait for the `serve-root` port to become free before starting the next benchmarked runner so transient web-server shutdown races do not fail the harness.
- When manually launching Chromium in benchmark scripts for this repository, close it in a `finally` block so failed runs do not leak headless Chrome processes.
- When a user sets a hard FPS floor for this project, enforce it with an automated browser render benchmark instead of relying on visual impressions or the throttled headless FPS counter.
- In this project, if SSR brightens the HDRI sky unexpectedly, inspect the vendored SSR shader for extra env-map fallback or double-counting before merely lowering the user-facing intensity control.
- When a post effect appears to do nothing in this project, verify the actual parameter scale before assuming the effect is broken. SSR looked dead because its UI range was crushed by an extra `0.02` multiplier, and N8AO looked dead because it was configured with an almost invisible 2px screen-space radius.
- When an optional third-party rendering package breaks the current `three` build, do not force it in just to satisfy a menu label. Keep the dropdown to the modes that are actually compatible and verified in this repository.
- When billboarded effects live under rotated parent groups, compute their local billboard quaternion from the parent's world rotation instead of copying the camera's world quaternion directly.
- When restricting SSR in this repository to PBR materials, hide unsupported meshes during the SSR g-buffer pass and restore them afterward; merely skipping the MRT material swap is not enough.
- In this project, `@react-three/postprocessing` lens flares use `opacity` as suppression, while `colorGain` and the flare sizes control the visible strength. Drive the UI accordingly.
- When the user asks to expose post-effect controls, wire the exact underlying effect parameters into the debugger instead of relabeling a generic intensity slider.
- When proving a rendered prop is visible, isolate the prop's own shell in the screenshot check; a cap highlight or a coincident shadow is not enough evidence.
- When the user reports that a rendered object is absent, do not reframe the issue as a perception or occlusion problem without proof. First rule out implementation differences that can make the object fail to render at all.
- When a visual dispute remains unresolved, have the smoke test persist the exact screenshots used for the proof with stable filenames so the human can inspect the same rendered states.
- When a scene layout becomes randomized, add a deterministic query-param hook for browser tests so smoke and perf runs can target one persisted scene and stay stable.
- When a requested content replacement removes a rendering system entirely, uninstall the now-unused package instead of leaving it as dead dependency drift.
- When timing Playwright-based benchmarks in this repository, treat both `test:smoke:runner` and `test:perf:runner` as users of the same local web-server port and wait for that port to be free before launching either runner.
- Do not launch multiple Playwright jobs in parallel in this repository when they share the fixed `serve-root` port. They will race on `127.0.0.1:42731` and produce false failures.
- The static-root Playwright runners in this repository exercise the already-published root bundle, not live source files. Re-run `npm run build:pages` before trusting their results after source edits.
- When a larger maze raises the torch count, profile live point lights and shadow casters before changing materials or geometry. In this repository the fastest fix was limiting shadow casting to the nearest torches and culling distant point lights, not blindly downgrading the whole scene.
- In this repository, default ambient occlusion is expensive enough to break the `>=120 FPS` gate in the maze scene. Keep AO off by default and let the debug panel enable it on demand.
- Fixed-size baked point-light shadow maps do not remove the per-frame lighting and shadow-sampling cost. Measure the all-lights version before assuming static shadow maps make light culling unnecessary.
- When reading keyboard state from a sparse key map in this project, treat missing keys as `false` before numeric conversion. `Number(undefined)` becomes `NaN` and can poison the whole movement state on the first frame.
- When temporarily evaluating a lighting configuration that is expected to violate the normal FPS budget, disable the automated FPS gate explicitly in code and docs rather than letting it fail noisily.
- When a user reports that only the HDRI skybox and flame billboards are visible, test the real maze wall material in the smoke harness. In this project that symptom points at the lit-material render path, not a missing texture URL or a misunderstanding about where the wall should be.
- When a random maze may affect a rendering bug, surface the active maze ID in the top-right overlay so browser reports can be tied to a specific persisted maze. Reserve `F9` for toggling that overlay without affecting gameplay behavior.
- When the task is to debug full-fidelity rendering rather than optimize, keep the requested full PBR material stacks and baked-all-shadows lighting enabled even if that temporarily hurts FPS. Compatibility simplifications should be treated as temporary diagnostics, not the default solution.
- When replacing realtime torch lighting with baked maze lighting in this project, remove the obsolete flicker and point-light controls from the runtime and tests instead of leaving a dead hybrid path behind.
- When a late baked-lightmap step makes maze generation slower, keep the under-100ms gate on the maze-topology phase and benchmark the bake separately instead of letting persistence loops wait forever on the old total-time threshold.
- When verifying baked wall lighting in this project, use a deterministic wall-facing view and a lit-wall crop rather than a broad maze shot that can mostly measure the floor.
- When verifying a torch-lit wall in this maze, make sure the camera is on the lit-cell side of the wall. An exterior wall's opposite face is correctly dark.
- When a rendering regression hits a requested feature in this project, do not remove the feature to make the symptom disappear. Preserve the requested behavior and repair the implementation path instead.
- When enforcing the smoke-test duration budget in this repository, keep the benchmark harness writing `logs/latest-test-benchmark.json` and `logs/latest-smoke-profile.json` and cut the largest measured phase first instead of guessing where the time went.
- When the benchmarked smoke runner starts mixing startup enforcement with slower end-to-end render coverage, split those concerns instead of quietly letting the benchmark drift. Keep a lean smoke runner for the enforced startup/control path and move heavier reflection or fog assertions into a separate integration spec.
- When local reflection probes in this repository become too expensive to block startup, bake the probes needed around the spawn area first and treat the rest as lower-priority follow-up work rather than stalling scene readiness on the full maze.
- When baking one-shot local reflection probes in this scene, require the full expected set of baked ground patches, maze walls, and sconces to be ready before capturing. Waiting for only one ready mesh can permanently bake missing occluders into the probes.
- When debugging local reflection probes in this project, inspect the actual HDR cubemap capture path before trusting a separate debug proxy. A low-res 8-bit probe preview can mislead you into thinking geometry or color is missing when the preview itself is the degraded path.
- When a reflection-probe regression appears in this project, do not accept a passing proof that only shows a probe toggle changing state. Add or use a direct capture-content check so the smoke test proves the probe actually contains nontrivial maze geometry and torch detail.
- When maintaining the reflection-probe debug view in this project, treat the debug spheres as a literal visualization of the probe contents for the human. Do not substitute PMREM output or any other processed approximation for the raw captured cubemap on those spheres.
- When debugging reflection probes in this project, inspect the actual cube-camera capture path before trusting a visual symptom. In particular, check the cube camera's enabled layers and any temporary capture-only light or emitter substitutions before concluding that geometry is missing from the probe.
- When reflection-probe geometry seems to disappear in this project, validate the exact debug and readback path before concluding that the capture scene is wrong. In this repo, an `UnsignedByteType` raw cube target currently gives the most reliable combination of capture, probe preview, and smoke-test instrumentation.
- When reflection-probe content is disputed in this project, use the raw-face atlas debug path rather than relying on a sphere screenshot alone. The sphere is useful for in-scene inspection, but the exact face atlas is the falsifiable proof of what the cubemap actually contains.
- When the user reports a rendering result they can directly see, do not treat indirect probes, internal counters, or partial debug paths as proof against that report. Validate the exact user-visible render path end to end before claiming the issue is understood or fixed.
- In this repository, the shipped revision label is baked into `assets/app.js` at build time from the current Git `HEAD`. If a release build should advertise a specific commit, create the commit first and then rebuild the pages asset, not the other way around.
- When reflection-probe capture success is disputed in this project, replace beauty-pass screenshots with a geometry-only cube-camera capture against a black background so the proof distinguishes "the cube camera sees maze geometry" from "later probe shading or filtering lost it."
- When a mesh may be failing to render in an offscreen pass in this project, do not treat geometry-only overrides, proxy previews, or indirect counters as disproof. Reproduce the exact pass with a stepwise material continuum, starting from a known-good material and adding one real-material feature at a time until the first failing step is identified.
- When the user reports a direct visual fact about the scene, treat that report as higher priority than my inference from a debug proxy. If I believe a proxy disagrees, the next step is to validate the exact user-visible or exact capture path, not to argue from the proxy.
- When adding or modifying any debug-panel control that is supposed to change visible rendering, do not stop at wiring UI state or debug objects. Before claiming it works, prove all of these: the UI value changes, the downstream shader/effect/pass state changes, the rendered image changes when the control is nonzero or enabled, and the rendered image does not change when the control is zero or disabled. A checkbox or slider that only changes React state, props, or debug readouts is not an implemented feature.
- When a visual control appears wired in code but may still be inert, inspect the final runtime binding point rather than the upstream state. For materials that means the compiled shader uniforms/textures; for post effects that means the live effect/pass instance; for scene toggles that means the actual rendered frame. Do not use intermediate state changes as completion criteria.
- When extending built-in three.js materials in this repository, use `customProgramCacheKey`, `onBeforeCompile`, and direct updates to the patched shader uniforms. Do not mutate renderer-internal uniform stores or assume a React/debug mirror proves the live GL program changed.
- When tracking unit-test performance in this repository, keep a checked log of per-file and per-subtest durations and enforce the 20-second budget from the TAP-reported test durations while still recording any extra wall-clock teardown overhead separately for investigation.
- When source-controlled runtime data becomes large in this project, do not route the browser through the same eager import path that Node tests use. Keep a separate browser runtime loader so heavy persisted payloads stay out of the initial bundle.
- When implementing a lighting toggle for baked lightmaps versus probe IBL, verify the lightmap's ambient channel and the runtime diffuse-I BL path both change. Adjusting only the torch-light intensity term is not a working toggle.
- When implementing a "Probe IBL" debug toggle intended to compare against baked surface lightmaps, make the toggle disable the wall and floor surface lightmap bindings themselves. Leaving any baked lightmap contribution active makes the toggle semantically wrong and visually ambiguous.
- When running a long script or test in this repository, make it report stage progress and per-stage timing so overruns are diagnosable before completion. Run it with a timeout comfortably above the measured ceiling, and treat any timeout overrun as a failure to investigate rather than a reason to blindly rerun the same opaque command.
- When persisted maze lighting is refreshed in this repository, always regenerate inspectable PNG artifacts for both the baked lightmaps and the reflection captures under `logs/lightmap-artifacts` so the human-readable artifacts stay in sync with the current maze payloads.
- When exporting baked HDR lightmaps in this repository, keep the human-readable preview PNG separate from the raw HDR PNG export. A tone-mapped preview is useful for inspection, but it must not masquerade as the raw runtime payload.
- When the human has already tuned a visual-balance control and I later change implementation details, do not silently retune compensating multipliers or tint factors. Preserve the previously adjusted visual balance unless the human explicitly asks for a new rebalance.
- When inspecting, exporting, or visualizing baked lightmaps or reflection probes in this project, keep them raw. Do not tone-map them, exposure-compensate them, or apply arbitrary gain multipliers in runtime shading, debug views, or artifact export paths.
- When baked maze walls or the maze-local lit ground patch already carry their diffuse ambient via baked lightmaps, do not let them fall back to runtime global-HDRI diffuse lighting when surface lightmaps are disabled for debugging. Probe IBL may replace that diffuse ambient, but global diffuse HDRI must stay disabled on that baked geometry.
- When a control only affects authored bake-time lighting in this project, do not expose it as a live runtime slider. Expose only runtime contribution controls in the debug panel, and keep bake-time lighting changes in the headless rebake path.
- When this project uses baked surface lightmaps plus local probes, keep the runtime controls split by contribution: surface lightmap intensity, probe diffuse IBL intensity, and probe reflection intensity. Do not collapse those into a single toggle that ambiguously changes multiple lighting paths at once.
- When adding browser debug helpers that set the camera in this project, ensure they also detach or override player-follow camera code. Otherwise the next frame can silently undo the test view and produce false render-test failures.
- When an asset is large but not needed to prove the first scene frame, keep it out of the Suspense-critical startup path and load it asynchronously after the maze can mount. The 4K fire flipbook should not block the loading overlay fade.
- When experimenting with multiple expensive postprocessing passes, keep the performance result as a first-class acceptance criterion. Five separate upstream lens-flare passes were functionally possible but not acceptable because they drove the render-integration scene down to about 2 FPS.
- When adding a new debug checkbox or slider, verify that it changes the live rendered image immediately, not just the React state or a debug mirror object. A control that does not produce an observable scene change is not done.
- After any long-running investigation in this repository, explicitly account for where the wall-clock time went and adjust the method if waiting dominates. Prefer narrow, isolated ablations and direct instrumentation over broad scripts that can burn minutes in opaque timeouts.
- When benchmarking or profiling rendering in this project, reject any run that reports SwiftShader or another software renderer. Use a hardware-accelerated browser path and verify the renderer string before trusting the numbers.
- When benchmarking feature costs in this project, first verify that toggling the feature does not reset persisted probe loading or any other heavyweight startup path. If a control change tears down runtime probe state, fix that before trusting the timing delta.
- When `npm run dev` becomes far slower or effectively frozen while the production build is responsive, treat Vite/React dev transforms as a separate suspect. In this repository, disabling React Fast Refresh is the correct fallback once the main app module is large enough to make dev refresh instrumentation pathological.
- When a startup freeze appears only on the interactive Vite server in this repository, compare both host and port instead of assuming the renderer changed. Keep the repo default on `127.0.0.1:5173`, and treat alternate ports only as temporary diagnostics rather than permanent redirects.
- When a maze is explicitly requested with `?maze=<maze-id>`, never silently fall back to a random different maze. Load the requested maze or fail clearly.
- When syncing or rebuilding persisted maze runtime data in this repository, always emit the inspectable baked-lighting artifacts under `logs/lightmap-artifacts/<maze-id>/` at the same time. The runtime `.bin` payload alone is not sufficient because lighting regressions must be debuggable from image files.
- When syncing `rgb16f` maze lightmaps in this repository, emit inspectable `surface-lightmap.png` and `surface-lightmap-rgbe.png` files directly under `public/maze-data/<maze-id>/` alongside `surface-lightmap.bin`. The public runtime directory itself must be debuggable without cross-checking another folder.
- Every persisted maze in this repository must emit the same baked-lighting artifact set in the same way. Do not let one maze retain stale log files or omit public inspection files that the others have.
- When the user explicitly asks for a long uninterrupted batch or says they are going to sleep, treat that as a hard instruction to continue autonomously until the requested batch is complete or a truly blocking external dependency is reached. Do not stop early just because one branch of the work is inconvenient or incomplete.
- When runtime gameplay lighting in this project depends on baked surface lightmaps or volumetric-lightmap probes, never let it silently fall back to global environment lighting. In-game fallback must be black so missing baked data is obvious instead of visually masked.
- When uploading runtime `rgb16f` lightmaps in this repository, use `RGBAFormat + HalfFloatType`, not `RGBFormat + HalfFloatType`. The RGB half-float path is not reliable on ANGLE and can manifest as black walls, frozen startup, and a flood of WebGL allocation errors.
- When runtime volumetric-lightmap or fog occlusion depends on baked probe depth, pack the persisted probe depth faces into shader-readable atlases and consume those atlases in the runtime probe-weighting path. Do not regress to an `L0`-only ambient texture if the requirement is wall-respecting local lighting.
- When the user asks for per-setting debug resets, make the double-click handler reset only that specific control. Do not wire a label to a whole-tab reset helper just because it is convenient.
- When a generation or validation loop can reject candidates, give it a bounded attempt count and a diagnostic failure. Do not allow a test fixture or bad factory to create an unbounded retry loop.
- When offline baking code needs the HDRI as an input, use the bake/capture scene source directly. Do not rely on runtime environment-light state, because in-game global environment lighting is intentionally disabled and may be null.
- Keep full reflection-probe export measured as an offline asset pipeline phase. Do not fold its multi-minute runtime into the fast maze topology, validation, and lightmap-budget measurements.
- When a real browser load freezes but tests pass, inspect whether the tests measured only startup readiness or disabled-effect states. Performance gates must cover the user-visible steady state after background work has stabilized.
- Do not let runtime probe loading silently accumulate every baked probe in GPU memory. Keep a bounded resident set near the player and assert that bound in browser tests.
- When an asset causes a frame-rate collapse, measure mesh count, material count, skinning, and draw submissions before reducing visual fidelity. A static skinned model split into many meshes should be joined offline into a runtime asset rather than hidden or replaced.
- When the user reports a freeze during asset load, add or run a live-RAF startup gate before declaring it fixed. Steady-state benchmarks after `data-loading-complete` are insufficient because they can miss the exact first-use stall the user sees.
- When browser startup needs a full-resolution authored texture, prefer an offline runtime derivative that removes unused pixels or heavyweight encodings while preserving the authored visual content. Do not make the browser decode 16-bit source PNGs or transparent margins when a generated runtime PNG can do the same job.
- When the scene uses the postprocessing composer by default, warm the composer passes before fade-out just like scene textures and mesh shaders. Otherwise the first visible gameplay frame can absorb the composer initialization cost even after mesh warmup passes.
- When a movement-path performance regression can make the game unplayable even though static-camera benchmarks pass, add a browser test that drives the recorded solution replay and samples GPU render cost during the replay.
- When gating fade-out on nearby volumetric-lightmap readiness, load coefficient/depth data separately from specular reflection textures so the diffuse/fog startup radius does not break the resident reflection-probe memory budget.
- Treat runtime levels as streaming, visibility, solution, and debug/load-save anchors rather than separate gameplay worlds. Ordinary walking across a level boundary must not swap independent rules state, held-item state, lighting globals, fog resources, or lens-flare source lists; those systems should operate on continuous world-space state and currently rendered resources.
- When verifying animation fixes in this project, do not infer success from state transitions or from removing a snap assignment. Add a browser assertion that samples the rendered object's world position across frames and proves an intermediate pose exists.
- When profiling frame time in this project, do not leave browser, compositor, or GPU work inside a generic "uninstrumented" bucket. Capture a Chrome trace during the actual gameplay path, parse it into a hierarchical tree, and normalize trace event costs to ms/frame with explicit self/untraced residual leaves.
- Chrome trace output is not enough to optimize rendering in this project. Maintain app-owned named render scopes around composer passes, custom pass sub-steps, WebGL submissions, and hot frame systems, and wait for startup/background loading to cool down before recording representative performance.
- When a performance report shows long frames during traversal, include tracked resource-count deltas on each long frame. A spike that coincides with reflection-probe or volumetric-probe residency changes is a streaming/upload clue, not evidence that ordinary cube/lightmap drawing costs that much.
- Startup readiness flags must not suppress later scene warmup after seamless level/layout changes. A one-time "basic assets ready" flag can coexist with per-scene shader/composer warmup, but it must not skip that warmup for the scene being profiled.
- Shared texture readiness flags should be owned by the shared asset, not by individual level component lifetimes. If multiple rendered levels mount the same atlas, one level unmounting must not clear the global "loaded" marker while the shared texture remains resident.
